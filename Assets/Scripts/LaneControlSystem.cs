using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LaneControlSystem : MonoBehaviour
{

    //Moves Cards across the lanes continuously. Manages the movement of cards, checking for the activatable moment, and changing states depending on turn.
    //Triggers Effects and Destruction of cards.
    //Assigns the card dropped on the lane to the correct space in the lane.





    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
